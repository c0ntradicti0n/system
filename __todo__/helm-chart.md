we have kompose installed. How is that, if we want to bake a helm chart from our compose.yml?

compose.yml

and deploy it to local kind cluster

document the steps needed to do that in scripts or targets of:

./justfile

our use a just submodule with the mod syntax.

would be soon great to deploy it in a separate namespace onto our remote cluster under another subdomain. "polarity.science" as we had it.
